#include "monty.h"
/**
 * nop - doesn't do anything.
 * @stack: pointer to pointer to the top of stack
 * @line_number: the line number of nop instruction
 *
 * Return: void
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
